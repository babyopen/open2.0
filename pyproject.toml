[project]
name = "zodiac-lottery-predictor"
version = "0.2.0"
description = "预测 API"

[project.scripts]
app = "api.index:app"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
